class SlotSelectionHandler {
  double nearestBookedSlotLeft;
  double nearestBookedSlotRight;

  SlotSelectionHandler({
    this.nearestBookedSlotLeft = 0.0,
    this.nearestBookedSlotRight = 0.0,
  });
}
